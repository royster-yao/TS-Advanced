/**
 *  @数组与元组
 */

let arrOfNumbers: number[] = [1,2]
// arrOfNumbers.push("123") // error
arrOfNumbers.push(3)
function test(){
    console.log(arguments);
    // let arr: any[] = arguments
    // let htmlCollection: HTMLCollection
}

let user: [string, number] = [`royster`,1]
user.push(12) // 只能push以上两种中的一种 

export default {}